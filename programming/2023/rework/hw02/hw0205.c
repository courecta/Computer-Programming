#include <stdio.h>
#include <stdint.h>

void catan(int64_t length, int64_t number);

int main()
{
  int64_t length = 0, number_layer = 0;
  printf("Please input the length: ");
  scanf("%ld", &length);
  printf("Please input the number of layer: ");
  scanf("%ld", &number_layer);

  catan(length, number_layer);
  
  return 0;
}

void catan(int64_t length, int64_t number){



  return;
}